<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{name}} Details</title>
    <link rel="stylesheet" href="/css/reset.css">
    <link rel="stylesheet" href="/css/header.css">
    <link rel="stylesheet" href="/css/main.css">
    <link rel="stylesheet" href="/css/footer.css">
    <link rel="stylesheet" href="/css/button.css">
    <link rel="stylesheet" href="/css/product-card.css">
    <link rel="stylesheet" href="/css/product-details.css">
</head>

<body>
    {{> header}}

    <main class="site-main">
        <header>
            <h1>{{product.name}} Details</h1>
        </header>

        <article class="product-card">
            <header class="card-header">
                <h1>{{product.name}}</h1>
            </header>

            <div class="card-img">
                <img src="{{product.imageUrl}}" alt="{{product.name}}">
            </div>

            <div class="card-content">
                <h2 class="product-price">£{{product.price}}</h2>
                <p class="product-description">{{product.description}}</p>
            </div>

            <div class="buttons-container">
                <form action="/admin/products/delete/{{product._id}}" method="POST" class="form-group">
                    <button type="submit" class="button">Delete</button>
                </form>

                <form action="/cart" method="POST" class="form-group">
                    <button type="submit" class="button" id="add-to-cart-btn">Add To Cart</button>
                    <input type="hidden" name="productId" value="{{product._id}}">
                </form>

                <a href="/admin/products/edit/{{product._id}}" class="button">Edit</a>

            </div>
        </article>
    </main>

    {{>footer}}
</body>

</html>